<div class="container">
    <div class="row my-2">
        <div class="col-md-4">
            {{#each books}}
            <div class="card">
                <div class="card-body">
                   <h2>{{title}}</h2>
                    <p>{{description}}</p>
                </div>
            </div>
            {{/each}}
        </div>
    </div>
</div>